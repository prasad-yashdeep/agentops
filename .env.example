# ═══════════════════════════════════════════════════════════════
# AgentOps Configuration
# Copy this to .env and fill in your keys
# ═══════════════════════════════════════════════════════════════

# ─── Anthropic (Claude) ──────────────────────────────────────
# Powers AI-driven root cause analysis and fix generation.
# OPTIONAL: Without this, a rich rule-based diagnosis engine
# handles everything. Add it to unlock LLM-powered reasoning.
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-20250514

# ─── Blaxel — Persistent Sandboxes (YCX25) ──────────────────
# Cloud sandboxes for isolated fix testing. Resume in <25ms.
# Get your key at: https://app.blaxel.ai → Settings → API Keys
BLAXEL_API_KEY=
BLAXEL_WORKSPACE=       # Your Blaxel workspace name
USE_LOCAL_SANDBOX=true  # Set to 'false' when BLAXEL_API_KEY is set

# ─── White Circle AI — Safety Layer ─────────────────────────
# Validates agent outputs before deployment. Catches dangerous fixes.
# Falls back to built-in safety checks if not set.
WHITECIRCLE_API_KEY=
WHITECIRCLE_API_URL=https://api.whitecircle.ai/v1

# ─── ElevenLabs — Voice Alerts ───────────────────────────────
# Generates spoken alerts for critical incidents.
# Get your key at: https://elevenlabs.io → Profile → API Keys
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM  # Rachel (default)

# ─── Agent Tuning ────────────────────────────────────────────
MONITOR_INTERVAL=5          # Seconds between health checks
AUTO_FIX_THRESHOLD=0.85     # Confidence above this → auto-deploy
ESCALATION_THRESHOLD=0.5    # Confidence below this → immediate escalation

# ─── Server ──────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000
